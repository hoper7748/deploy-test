name: deploy-study

on:
  push:
    branches: [main]
  pull_request: 
    branches: [main]

jobs:
  run1:
    runs-on: ubuntu-22.04  #가상 피시
    steps:
      - name: "첫번째 작업"
        run: echo "출력"
      - name: "두번째 작업"
        run: | 
          cd ~/
          ls
          mkdir test-item
          ls
      - name: Build and push Docker images
        uses: docker/build-push-action@v7.3.0
      - name: Setup Java JDK
        uses: actions/setup-java@v5.6.0
      - name: Checkout
        uses: actions/checkout@v6.1.0
        with:
          ref: main  # 브랜치 선택 해당 브랜치를 체크아웃 하겠다.

      - name: "세번째 작업"
        run: |
          cd ~/
          ls
